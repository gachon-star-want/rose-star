# Decision Log

이 문서는 토론 중 확정된 결정을 기록한다. grill-me 방식으로 한 번에 하나의 질문을 해결한다.

## 결정됨

| ID | 결정 | 근거 | 날짜 |
| --- | --- | --- | --- |
| D001 | 공모전 목표는 1.44MB 이하 독립 실행 게임 | 원문 규칙 | 2026-05-26 |
| D002 | 초기 검토 장르는 2D 타일 플랫포머였다 | 이후 D011로 대체됨 | 2026-05-26 |
| D003 | 개발 언어는 어셈블리어를 중심으로 한다 | 공모전 제약과 기술적 어필이 강함 | 2026-05-26 |
| D004 | 최종 릴리즈는 정확히 1,474,560바이트를 채운다 | 3.5인치 HD 플로피 한 장을 1바이트도 남기지 않는 것이 프로젝트 핵심 미학 | 2026-05-26 |
| D005 | 최종 실행 파일 형식은 Windows PE `.exe`로 한다 | 독립 실행 파일 조건을 가장 안전하게 만족하고 심사 환경 설명이 쉬움 | 2026-05-26 |
| D006 | Windows PE 아키텍처는 x86 32비트로 시작한다 | 작은 바이너리 가능성, Win32/FASM 자료, 단순한 호출 관례, WOW64 호환성 | 2026-05-26 |
| D007 | 어셈블러는 FASM으로 한다 | PE 직접 생성, 링커 부담 최소화, x86 Win32 예제 접근성 | 2026-05-26 |
| D008 | 내부 해상도는 256x144로 한다 | 16:9 비율, 16x16 타일 기준 16x9 화면, 낮은 렌더링 비용 (이후 D046으로 4:3 256x192로 갱신됨) | 2026-05-26 |
| D009 | 오디오는 1차 효과음, 2차 음악으로 나눈다 | 효과음은 피드백에 필수지만 음악은 게임 루프 이후 exact-fill 컨텐츠로 적합 | 2026-05-26 |
| D010 | 32개 섹터 목표를 검토했다 | 이후 D012로 대체됨 | 2026-05-26 |
| D011 | 게임 장르는 고정 경로 기반 마이크로 타워디펜스로 한다 | 64개 스테이지와 어셈블리 구현에 더 적합하고 동적 물리보다 완성 리스크가 낮음 | 2026-05-26 |
| D012 | 최종 스테이지 목표는 8x8 구조의 64개 스테이지로 한다 | 1.44MB는 Mario 40KiB의 36배이며, 타워디펜스는 명령 기반 데이터로 많은 스테이지를 담기 좋음 | 2026-05-26 |
| D013 | 스테이지 데이터는 오브젝트 명령 기반 + RLE 보조로 저장한다 | 전체 타일맵 저장보다 작고 64개 스테이지 변주에 적합함 | 2026-05-26 |
| D014 | 타워디펜스 경로 모델은 고정 경로로 한다 | 동적 길찾기와 막힘 판정을 피하고 64스테이지 데이터 밀도에 집중 | 2026-05-26 |
| D015 | 도트 그래픽은 리소스 재사용 중심으로 만든다 | 8x8/16x16 바디, 팔레트 교체, 2프레임 애니메이션, 행동 플래그로 다양화 | 2026-05-26 |
| D016 | 타워/적 목표는 1차 타워 3종/적 4종, 최종 타워 5종/적 8종으로 한다 | 범위와 슬로우를 분리해 단일/강공격/범위/슬로우/지원 역할을 만든다 | 2026-05-26 |
| D017 | 64스테이지는 순차 해금 곡선을 가진다 | 디스크 단위로 새 타워/적/규칙을 풀어 학습 흐름을 만든다 | 2026-05-26 |
| D018 | 경제 모델은 스테이지별 고정 시작 자원, 소량 처치 보상, 판매 60% 환급으로 한다 | 스테이지별 퍼즐성과 실수 복구를 동시에 확보 | 2026-05-26 |
| D019 | 이자는 보유 자원 비례가 아니라 초당 n BYTES 지급 모드로 둔다 | Easy Mode 난이도 완화 장치로 사용, Hard Mode도 클리어 가능해야 함 | 2026-05-26 |
| D020 | 난이도 표기는 Easy Mode와 Hard Mode로 한다 | Easy는 초당 자원 생성 있음, Hard는 초당 자원 생성 없음 | 2026-05-26 |
| D021 | 클리어 기록은 Easy Mode와 Hard Mode를 구분한다 | Hard 클리어는 챌린지 성취, Easy 클리어는 완화 모드 성취로 표시 | 2026-05-26 |
| D022 | 최고기록 공유는 온라인 리더보드 대신 공유 가능한 기록 코드로 한다 | 네트워크/서버 없이 텍스트와 체크섬으로 공유 가능 | 2026-05-26 |
| D023 | 결과 화면에서 버튼 하나로 1080x1080 공유 카드 이미지를 저장한다 | Instagram 공유를 쉽게 하되 자동 업로드/네트워크 연동은 피함 | 2026-05-26 |
| D024 | 공유 카드 저장은 1차 BMP, PNG는 보너스 목표로 한다 | BMP writer는 작고 실행 후 생성 파일은 제출물 크기에 포함되지 않음 | 2026-05-26 |
| D025 | 결과 화면에 공유/복사/재시도 키 안내 팝업을 둔다 | 버튼 발견 가능성을 높이되 긴 설명 UI는 피함 | 2026-05-26 |
| D026 | 공유 카드 중앙에는 큰 게임 스냅샷을 배치한다 | 현재/결과 화면을 픽셀아트로 확대해 공유 카드의 주 시각 요소로 사용 | 2026-05-26 |
| D027 | 공유 카드 스냅샷은 클리어 순간의 결과 화면 직전 프레임버퍼를 사용한다 | 최종 배치가 보이고 결과 UI가 스냅샷을 가리지 않음 | 2026-05-26 |
| D028 | 스테이지 데이터 포맷은 Header + path commands + build zones + waves + flags로 한다 | 64개 스테이지를 작게 저장하고 Easy/Hard를 같은 데이터로 공유 가능 | 2026-05-26 |
| D029 | 64스테이지 x Easy/Hard로 실질 128개 챌린지를 제공한다 | 맵 데이터 복제 없이 난이도 모드 차이로 재도전 가치를 만든다 | 2026-05-26 |
| D030 | Hard Mode는 팔레트 반전/고대비 변형으로 시각적으로 구분한다 | 새 이미지 없이 난이도 차이를 즉시 보이게 함 | 2026-05-26 |
| D031 | 첫 구현 마일스톤은 1스테이지 최소 루프로 한다 | Bit Gun, Bit enemy, 고정 경로, Easy/Hard 경제 차이, Hard 팔레트까지 검증 | 2026-05-26 |
| D032 | 컨셉/타워/적/레벨 디자인을 별도 상세 문서로 분리한다 | 구현 전 참조 가능한 수치와 콘텐츠 구조를 고정하기 위함 | 2026-05-26 |
| D033 | 구현 시작 전 첫 작업은 FASM 확보 후 Windows x86 PE 빈 창 + 256x144 프레임버퍼 출력으로 한다 | 게임 로직보다 먼저 빌드 가능한 exe와 화면 출력 경로를 증명해야 함 | 2026-05-26 |
| D034 | Windows exe 실행 검증은 개발 중 Wine smoke, 최종 Windows 실기기/VM 검증으로 이중화한다 | Wine은 빠른 반복에 좋지만 최종 심사 환경을 완전히 대표하지 않음 | 2026-05-27 |
| D035 | FASM 확보 방식은 프로젝트 로컬 고정 툴체인 + 버전/해시 기록으로 한다 | 시스템 설치 상태에 의존하지 않고 빌드 재현성을 확보하기 위함 | 2026-05-27 |
| D036 | 표준 빌드 명령은 Makefile을 공개 인터페이스로 두고 내부는 `tools/` 스크립트로 분리한다 | 사용법은 단순하게 유지하고 복잡한 검증 로직은 교체 가능하게 분리하기 위함 | 2026-05-27 |
| D037 | Matt Pocock 계열 스킬 사용을 위해 repo agent 설정을 먼저 잡는다 | PRD, issue, triage, TDD, diagnose 스킬이 issue tracker와 domain docs 위치를 알아야 함 | 2026-05-27 |
| D038 | root agent instruction file은 `AGENTS.md`로 만든다 | Codex 프로젝트 기준으로 가장 직접적인 agent instruction 파일임 | 2026-05-27 |
| D039 | 다음 실제 스킬은 `grill-with-docs`로 하고 루트 `CONTEXT.md` 도메인 용어를 먼저 고정한다 | 구현 전에 Stage/Sector/BYTES 같은 언어를 확정해야 이후 PRD, issue, 테스트 이름이 흔들리지 않음 | 2026-05-27 |
| D040 | 다음 계획 산출물은 `to-prd` 전에 `.scratch/tracer-bullet/` 첫 playable slice 이슈 세트로 만든다 | 현재 최대 리스크는 전체 PRD 문장보다 FASM/Win32/프레임버퍼/입력/빌드가 실제로 닫히는지 검증하는 것임 | 2026-05-27 |
| D041 | 첫 구현 진입점은 `01-project-skeleton.md`로 하고, 완료 기준은 A(디렉터리 구조 + Makefile 타깃 + size/toolchain-check 보고까지, 실제 exe 빌드 불요)로 한다 | 01은 FASM 없이 100% 닫을 수 있는 유일한 이슈이며, 실제 빌드는 의존성상 02(FASM)→03(PE 창)에서 닫는 것이 맞음 | 2026-05-31 |
| D042 | FASM 툴체인은 네이티브 vendored FASM 1.x macOS(x86_64) 바이너리를 `tools/toolchain/`에 고정 + Rosetta 실행 + SHA256 기록을 1순위로, Docker+Linux FASM을 문서화된 fallback으로 한다 | D035와 일치하고 fasm 1.x는 `format PE` 내장이라 Win32 예제와 맞음. `format PE`는 호스트 아키텍처와 무관하게 32-bit Windows PE를 출력하므로 x86_64 호스트 바이너리로도 문제 없음 | 2026-05-31 |
| D043 | 1,474,560바이트 제한은 제출물(`dist/`의 exe)에만 적용되고 FASM/Docker/Makefile/tools 등 빌드 도구 용량은 제한과 무관하다 | 제한 대상은 완성 게임 산출물이며 빌드 공장은 제출물에 포함되지 않음(D035/D036 재확인) | 2026-05-31 |
| D044 | 프로젝트를 git repo로 초기화하고, 빈 디렉터리는 각 디렉터리의 짧은 README.md placeholder로 추적한다. dist/와 build/ 산출물은 .gitignore로 제외한다 | exact-fill 회귀 추적(git diff/bisect), PixelLab MCP의 git 전제, Matt Pocock 워크플로와 정합. README placeholder는 .gitkeep보다 자기설명적 | 2026-05-31 |
| D045 | 빌드/툴체인/git 등 프로그래밍 인프라 결정은 Executor가 합리적 최적값으로 확정해 plan/decision-log에 기록하고, 사용자 grill은 게임 설계(룰/타워/적/레벨/경제/펠)에 집중한다 | 사용자가 기술 인프라 위임을 명시적으로 요청. 설계 의사결정에 사용자 시간을 집중하기 위함 | 2026-05-31 |
| D046 | 표시 사양을 **4:3 / 256x192**(16x16 타일 → 16x12 그리드, 상단 UI 1행 제외 플레이 영역 **16x11**), **창 모드 전용**, **정수배 스케일 기본 4배(1024x768)** 로 하나의 결정으로 확정한다. D008(256x144/16:9)을 대체한다 | 고정경로 TD는 가로 화면이 표준이나 4:3은 세로 경로 스위치백 여지가 더 크고 레트로 CRT 감성에 부합. 창모드 전용+정수배 스케일은 어셈블리 구현을 단순화하고 풀스크린 비정수 확대(1080÷144=7.5배)로 인한 픽셀 뭉개짐을 피해 선명도를 보장. 144↔1.44 라임은 설계 근거가 아닌 매력 요소로만 취급. 추가 톤 방향: 레트로 도트 외형 + 현대적 내용/스토리(별도 세계관 grill에서 구체화) | 2026-05-31 |
| D047 | 64맵 다양성의 **1순위 레버는 병목(chokepoint) 설계**(경로와 빌드존을 한 쌍으로)로 한다. 적 구성 다양성은 **Disk 테마**가, 자원(시작금/수입)은 **난이도 다이얼**이 담당한다 | 고정경로 TD 정석은 맵당 의미있는 병목 2~4개(좁은 16x11에선 1~2개)로 화력 집중점을 만드는 것. 경로가 좁아지는 곳이 곧 가치있는 빌드존이므로 둘은 별개 레버가 아니라 병목을 만드는 한 쌍. 타워 역할(단일/AoE/장거리)이 병목에서 분화됨 | 2026-06-01 |
| D048 | 게임 세계관을 **어린왕자(Le Petit Prince) 모티프 기반**으로 채택한다. 캐릭터·서사를 차용하되 80-90s 도트로 재해석하고, 원작 삽화 기반으로만 만든다(영화/애니판 디자인 모방 금지, 한국어 번역 문장 인용 금지) | 한국 한정 공모전 기준 법적 안전 확인: 생텍쥐페리 사후 50년(1994) 저작권 만료로 한국에선 퍼블릭 도메인, 2009 특허심판원이 "어린왕자" 제호·삽화를 관용표장으로 보아 상표 독점 부정, CONTEST.md에 IP 금지 조항 없음. 바오밥 싹 뽑기=행성 방어가 원작 차원에서 TD 메커니즘과 일치. 해외 배포 시에만 미국2039/프랑스2032 재검토 필요 | 2026-06-01 |
| D049 | 세계관 톤은 **순수 어린왕자 우화**로 한다(A안). 레트로(80-90s 도트·팔레트·CRT 질감)는 **시각 표현 레이어에만** 적용하고, 서사·세계관·용어 명명은 순수 동화로 간다. 기존 디지털 명명(BYTES 자원, Bit/Nibble/Block/Glitch 적, 플로피 백업 서사 액자)은 폐기하고 우화적 명명으로 교체한다. 표준 한국어 표기는 **"바오밥"**(바오바브 아님) | 사용자가 "보이는 것만 레트로"를 원함 — 디지털 SF 액자 없이 순수 동화 내용에 레트로 외형만 입힌다. 1.44MB exact-fill(D004)은 서사와 무관한 기술 제약으로만 유지 | 2026-06-01 |
| D050 | 코어(지켜야 할 것)=**장미** 한 송이, 플레이어 분신=**어린왕자**로 한다. 바오밥 웨이브가 장미에 도달하면 꽃잎(생명)이 떨어지고 마지막 꽃잎이 지면 패배 | 장미는 원작의 감정적 핵심("길들인 것에 대한 책임")이라 방어 동기가 강하고 꽃잎=생명 표현이 즉각적. 어린왕자는 능동적 수호자이므로 플레이어 주체로 적합(타워가 아님) | 2026-06-01 |
| D051 | 주적 바오밥을 **4성장단계**(🌱싹→🌿새싹→🪴묘목→🌳거목)로 한다. 단계가 오를수록 체력↑·이동속도↓·처치보상↑(탱커화). 진행에 따라 높은 단계가 주력이 되고 **최종 스테이지는 거목 stage(보스)**로 한다 | 원작 "바오밥은 작을 때 뽑아야 한다" 교훈을 성장 곡선으로 구현 — 작은 싹은 싸고 빠르게 처리, 방치한 거목은 막기 힘든 탱커가 됨 | 2026-06-01 |
| D052 | 64스테이지를 **2디스크씩 4막(Act)**으로 구성한다. 막마다 주력 바오밥 단계가 한 칸씩 상승(Act1 싹 / Act2 새싹 / Act3 묘목 / Act4 거목), 모든 단계는 상시 등장하되 주력만 변동. **최종 64번째(8-8)는 거대 거목 보스 stage**. 기존 디스크 테마(디지털 명명)는 이 막 구조로 재편한다 | 막마다 새 단계 도입으로 D017 순차 학습 곡선과 정합. 4단계↔8디스크(2개씩) 정수 분할 | 2026-06-01 |
| D053 | 적 8종 = **바오밥 4단계**(🌱싹/🌿새싹/🪴묘목/🌳거목) + **특수 4종**: 🐍뱀(빠른 암살자, 사거리 회피·장미 직행), 🎩허영쟁이(주변 바오밥 강화 오라), 💼사업가(처치 보상 없음/자원 교란), 🍷술꾼(불규칙 이동, 슬로우·예측사격 저항). *(잠정 — 점등인/왕으로 교체 여지 있음)* | 바오밥은 물량/맷집 곡선, 특수 4종은 각기 다른 대응(우선순위/타게팅/경제/명중)을 강제해 전술 다양성 확보. 전원 원작 캐릭터로 우화 톤 유지 | 2026-06-01 |
| D054 | 타워 5종 = 🐑**양**(단일 근접, 바오밥 천적) / 🌋**화산**(범위 AoE) / 💡**가로등**(장거리 저격) / 🏜️**사막바람**(슬로우) / 🦊**여우**(주변 타워 버프). 1차 3종=양·화산·가로등, 최종 추가 2종=사막바람·여우 | 기존 타워 역할(단일/AoE/장거리/슬로우/버프, D016)을 원작 요소로 1:1 매핑. 양=바오밥 천적이라 기본 방어가 원작 그대로. 양에 "장미도 먹는" 리스크 메커닉 여지 | 2026-06-01 |
| D055 | 자원(타워 소환·강화 화폐, 기존 **BYTES** 대체)을 **✨별빛(Starlight)** 으로 한다. 적 처치·스테이지 시작 보너스로 획득(D018), 타워 소환·업그레이드에 소비. UI 표기 "별빛", 공유 카드 영문 "Starlight", Easy Mode 초당 지급(D019/D020)은 "초당 별빛"으로 표기 | 어린왕자는 별에서 왔고 "별을 바라보면 웃게 된다"가 원작 정서의 핵심. 💼사업가(D053)가 "별을 세어 소유한다"는 원작 설정과 직결돼 사업가의 보상 없음/자원 교란이 자연스럽게 설명됨. 적을 처치하면 갇힌 별빛이 하늘로 돌아오고 그 별빛으로 수호자를 부른다는 서사. 2글자라 좁은 상단 UI에도 적합 | 2026-06-01 |
| D056 | **메인 캠페인(이후 D059로 44해넘이=4달×11 개편) 세계관 프레임 = B-612 정주 방어.** 무대는 어린왕자의 별 B-612 한 곳 고정, 🌹장미 한 송이(D050) 사수. 바오밥이 막마다 더 자라 별을 위협(D052). 특수 4종(🐍뱀/🎩허영쟁이/💼사업가/🍷술꾼)은 "다른 별 어른들의 헛됨·탐욕·도피가 B-612까지 번져온 그림자"로 재해석(어린왕자가 *방문하는* 원작 주민 → *침범해오는* 위협으로 전환). 8 Disk = 공간 이동이 아니라 위협이 깊어지는 8개의 장(章). 보너스 모드(Endless/Random)는 메인과 구분되는 별도 프레임을 둔다(세부 D057) | 사용자가 정주 방어를 택하고 보너스는 다른 프레임을 원함. 장미 한 송이(D050)·바오밥 성장 단일무대(D052)와 완전 정합하며, 원작 소행성 순회의 매력은 메인을 흔들지 않게 보너스 모드로 분리 흡수 | 2026-06-01 |
| D067 | **Track A 프로젝트 스켈레톤 구현 + git 도입.** `git init`(코드 프로젝트 표준, 인프라 자율 D045). 디렉터리 `src/ tools/toolchain/ build/ dist/`(+.gitkeep), 루트 `Makefile`(`toolchain-check`/`build`/`size`/`release-check`/`clean`/`help`), `.gitignore`, `tools/toolchain/README.md`(FASM 위치·버전/SHA256·제출 제외), `src/README.md` 생성. 산출물명 **`dist/rose-star.exe`**. FASM 미설치라 `toolchain-check`/`build`는 명확히 실패 리포트(전역 어셈블러 사용 안 함, 확보는 이슈 02). `make size`/`release-check`는 dist 0B → 1,474,560 대비 정상 동작 검증됨. tracer-bullet PRD+이슈 02~06을 현재 결정(어린왕자·256x192·해넘이·🐑양·별빛·꽃잎5·키보드+마우스·44)으로 동기화. **이슈 01 수락 기준 전부 충족(done)**. 미커밋(사용자 요청 시 커밋) | 이슈 01(D026/D027)의 구체 실행. 빌드 인터페이스를 얇게(Makefile)·로직은 tools/로(D021/D022), exact-fill 검증(D004)을 빌드 리듬에 내장 | 2026-06-01 |
| D066 | **입력 = 키보드 + 마우스 둘 다 1차 지원(기본).** 마우스: 커서 이동, **좌클릭**=타워 설치/선택, **우클릭**=취소/판매, 타워 종류=상단 UI 아이콘 클릭. 키보드 동등 매핑 유지(방향키 커서, Z/Space/Enter 설치·선택, X/Backspace 취소·판매, Tab 종류 전환, R 재시작, Esc 종료). 좌표는 16x16 타일에 매핑(Win32 WM_MOUSEMOVE/WM_LBUTTONDOWN/WM_RBUTTONDOWN). **기존 03 "마우스 필수 아님·키보드 전용" 방침을 개정** | 사용자가 키보드+마우스를 기본 플레이 방식으로 지정. 고정경로 TD는 포인트-클릭 배치가 자연스럽고, Win32 마우스 메시지는 OS 제공이라 어셈블리 구현 부담이 작음. 키보드 동등 매핑으로 접근성·테스트 단순성도 유지 | 2026-06-01 |
| D065 | **별도 튜토리얼 없음 — 슈퍼마리오 1-1식 자연 학습.** 튜토리얼 화면·설명 텍스트·강제 가이드를 만들지 않는다. 대신 달1(해넘이 1-1~1-11)을 안전한 학습 곡선으로 설계해 플레이하며 조작·규칙을 익히게 한다: 1-1은 🐑양 1종·짧은 경로·약한 🌱싹 소수로 "설치→방어"가 저절로 이해되고, 이후 새 요소(적/타워/경로 변주)를 한 번에 하나씩만 도입(D017/03 해금 곡선). 짧은 키 힌트(09 키 힌트)만 허용 | 사용자가 마리오식 무(無)튜토리얼 온보딩을 원함. 별도 튜토리얼은 용량·번역·흐름 부담이고, 잘 설계된 첫 스테이지가 더 좋은 교사. 03/09의 "긴 튜토리얼 텍스트 금지" 방향과 정합 | 2026-06-01 |
| D064 | **별빛 경제 = 넉넉한 시작 + 작은 처치보상.** 시작 별빛: 달1/2/3/4 = **60/90/130/180**(각 해넘이 ±10 튜닝 허용). 처치보상: 11 적 표대로(🌱1/🌿2/🪴4/🌳7/🐍4/🎩4/💼0/🍷3 별빛). **Easy = 초당 +2 별빛, Hard = 0**(D019/D020). 판매 환급 60%(D018 유지). 타워 값은 10대로(양20/사막바람35/화산40/가로등45/여우50) | 미리 계획해 배치하는 퍼즐성(D018)에 맞게 시작을 넉넉히, 처치보상은 보조. 달별 램프로 새 타워 해금과 비용 상승을 흡수. 12 "밸런스 기준값"에 박아 AI가 해넘이별 웨이브만 채우면 되게 함 | 2026-06-01 |
| D063 | **장미 꽃잎(생명) = 모든 해넘이 고정 5장.** 바오밥이 장미에 닿으면 leak_damage만큼 꽃잎이 진다(11 적 표: 🌳거목·🐍뱀 2장, 나머지 1장). 0장이 되면 패배("별이 어두워졌다"). 난이도 곡선은 꽃잎 수가 아니라 적 수/체력/웨이브로 조절 | 단순·예측가능하고 원작 장미의 연약함과 맞는 적은 수(실수 2~4회 허용). 해넘이별 가변은 데이터·밸런스 짐만 늘림. Header의 petals 필드는 5 고정값으로 채움(가변 여지는 남김) | 2026-06-01 |
| D062 | **모드 표기 = 쉬움/어려움**(화면·결과), 공유 카드·기록 코드엔 `E`/`H` 문자 유지(D020 의미 불변). **승리 = "별을 지켰다" / 패배 = "별이 어두워졌다"**(서정적 톤). 전체 player-facing UI 문자열을 09 "UI 문자열 표 (확정)"에 박제(D061 글리프 서브셋 원천). 03 공유 카드 mock·캡션도 한글 표기로 동기화 | 한글 모드명이 한글 UI(D061)와 일관되고 "쉬움/어려움"은 직관·짧음·글리프 적음. 서정 승패 문구가 원작 정서·장미 방어의 무게와 가장 어울림. AI 구현용 스펙이라 모든 문자열을 한 표에 확정 | 2026-06-01 |
| D061 | **UI 문자열 = 한글 기본 + 사용 음절 서브셋 비트맵 폰트.** 모든 player-facing 문구는 한글(우화 톤), 실제 게임에 등장하는 음절만 비트맵화해 폰트 용량을 최소화(수백 글리프가 아닌 사용분만). 단축키 토큰([S]/[R]/[ESC])·기록 코드·파일명·내부 식별자만 ASCII 유지. 확정된 전체 문자열은 09의 "UI 문자열 표"에 박제하며 이 표가 폰트 글리프 서브셋의 원천이 된다. D020의 Easy/Hard 모드 의미는 유지하되 화면 표기는 한글화 가능(D062에서 확정) | 한국 한정 제출(D048)·순수 우화 톤(D049)에 맞고, exact-fill(D004) 예산상 서브셋 한글 비트맵 폰트는 부담 없음(오히려 채움 기여). AI 구현 관점에서 "사용 음절만 비트맵"은 명확한 작업이고, 문자열을 09에 모두 박으면 글리프 집합이 결정됨 | 2026-06-01 |
| D057 | **보너스 모드 프레임 분리.** Endless = 🏮**점등인의 별**(자전이 너무 빨라 쉴 새 없이 등을 켜고 끄는 "끝없는 반복"의 별 → 무한 웨이브와 메타포 일치, 💡가로등 타워와 직접 연결). Random = 🏜️**사막의 신기루**(매번 다른 모양으로 떠오르는 사막 길 → 절차적 생성 맵). 메인(B-612 정주, D056)과 시각·서사로 구분되며 각각 팔레트+라벨+기존 타일 재사용 스킨으로 표현 | 메인의 정주 방어와 달리 무한성/가변성을 원작 모티프(점등인의 끝없는 반복, 사막의 신기루)로 표현해 모드 정체성을 분리. **1.44MB exact-fill(D004) 영향 무시 가능**: 프레임 하나는 팔레트(~수십 B)+라벨+재사용 타일이라 <1KB, 오히려 채움 목표에 기여(04 exact-fill 3항). 실제 비용 동인인 Random 절차적 생성기는 D028에서 이미 수용·격리됨(프레임 명칭과 무관) | 2026-06-01 |
| D058 | **게임 제목 = "어린왕자: 장미의 별".** 기존 가제 **"Floppy Defense"** 폐기. "어린왕자" 제호로 인지도 확보(D048 관용표장이라 상표 자유) + "장미의 별"이 방어 대상(B-612의 🌹장미)을 명시. player-facing 명칭(CONTEXT.md, 09, 공유 카드 "FLOPPY DEFENSE" 문자열 등) 일괄 교체 대상 | 순수 우화 톤(D049)에 맞고 도트/레트로 타이틀 화면에도 어울림. 디지털 가제를 우화 제목으로 전환 | 2026-06-01 |
| D059 | **레벨 구조 개편: 64→44스테이지 = 4달 × 11해넘이.** 한 스테이지 단위 명칭 **SECTOR→🌇해넘이**(원작 "하루 해넘이 44번" 모티프), 8묶음 단위 **DISK→🌙달**. 구조: **달 = 막(Act) = 바오밥 주력 성장단계** 1:1 (달1🌱싹/달2🌿새싹/달3🪴묘목/달4🌳거목), 각 달 11해넘이, **44번째 = 거대 거목 보스**. 보너스 해금: **Endless = 22번째(달2 끝) 클리어**, **Random = 33번째(달3 끝) 클리어**. 해금 판정은 해당 해넘이 Easy 클리어(D028 원칙 유지). 기존 8디스크+4막 이중 레이어를 "달=막" 한 겹으로 단순화. **D052(8×8/4막 64), D028(해금 24/48), D046(1.44 라임 비-설계근거 단서)를 본 결정이 개정**한다 | 사용자가 원작 "해넘이 44번" + 공모전 1.44MB 라임을 제목이 아닌 캠페인 구조로 실현하기로 선택. 44=4×11은 디스크 레이어를 없애 바오밥 성장 4단계와 막을 직접 일치시켜 구조를 단순화하고 학습 곡선(D017)과 정합. exact-fill(D004)은 스테이지 수와 무관하게 유지(44스테이지도 04 예산상 여유) | 2026-06-01 |
| D060 | **디지털→우화 일괄 리네이밍 사전(+파생 숫자) 확정.** 용어: Floppy Defense→어린왕자:장미의 별(D058) · BYTES→별빛(D055) · FREE BYTES→장미 꽃잎(D050) · CORRUPT BYTES→바오밥(D051/53) · DISK(8)→달(4)(D059) · SECTOR→해넘이(D059). **"Stage"는 기획 일반어(영문)로 잔류**, player-facing 단위는 "해넘이". **파생 숫자**: "실질 128 챌린지"→**88**(44×2) · "64(개) 스테이지" 리터럴→**44** · 스테이지 주소 X-Y 체계 8×8→**4×11**(보스 8-8→**4-11**, 공유카드 "SECTOR X-Y"→"해넘이 달-번호"). 신규 player 문자열(SECTOR CLEAN→"해넘이 무사", CORRUPTION BLOCKED→"바오밥 막음" 등)은 **잠정** — 09 재작성 때 톤 확정. **유지**(디지털 아님): Path/Build Zone/Tower/Enemy/Wave/Record Code/Easy·Hard Mode/공유 카드. **불변**(기술용어): 02/04/05의 byte·bit 용량·PE 내용. sweep 대상: CONTEXT, 09(전면 재작성), 03, 10, 11, 12, 00, 04(부분), 07, 15, README, 14. 순서: CONTEXT+09 먼저 확정→나머지 전파 후 `grep "128\|64개\|8-8"` 완결성 점검 | 단일 사전으로 고정해 다수 문서 전파 시 표현이 흔들리지 않게 함. 파생 숫자/주소 체계는 용어 중심 sweep에서 누락되기 쉬워 명시 | 2026-06-01 |

## 보류

| ID | 질문 | 추천안 | 상태 |
| --- | --- | --- | --- |
| Q029 | 01 skeleton에서 src/, build/, dist/ 등 빈 디렉터리를 추적 가능하게 둘 방법은? (.gitkeep / placeholder / git 미사용) | 미정 | 질문 중 |

## 반려된 방향

| 방향 | 이유 |
| --- | --- |
| Unity/Godot 기반 | 런타임 크기로 1.44MB 제한을 맞추기 어려움 |
| 웹브라우저 게임 | 원문에서 웹브라우저 게임 금지 |
| DOSBox 전제 DOS 게임 | 독립 실행 파일 조건 해석이 위험함 |
| 대형 이미지/음원 리소스 | 용량 제한과 맞지 않음 |
| 2D 타일 플랫포머 | 타워디펜스 피벗으로 대체 |
| 32개 섹터 목표 | 64개 스테이지 목표로 상향 |

## 해결된 grill-me 질문

Q001. 최종 실행 대상 플랫폼을 **Windows PE `.exe`**로 확정할 것인가?

결정: **예.**

이유:

- 공모전 심사자가 실행하기 가장 무난한 형태다.
- 단일 exe로 제출 조건을 설명하기 쉽다.
- Win32 API만 쓰면 외부 런타임 없이 가능하다.
- DOS보다 현대 실행 환경 리스크가 낮다.
- macOS/Linux보다 심사 환경 가정이 보수적이다.

Q002. Windows PE 아키텍처를 **x86 32비트 exe**로 시작할 것인가?

결정: **예.**

이유:

- `x86 PE`: 바이너리가 더 작을 가능성이 있고 Win32 자료가 많다.
- `x64 PE`: 현대 Windows 네이티브 환경에 더 직접적이다.

현재 결정은 **x86 PE로 프로토타입을 먼저 만들고, 실행 환경 리스크가 보이면 x64 PE를 백업으로 비교**하는 것이다.

Q003. 어셈블러를 **FASM**으로 확정할 것인가?

결정: **예.**

이유:

- PE 실행 파일을 직접 만들기 쉽다.
- 작은 단일 파일 어셈블리 프로젝트와 잘 맞는다.
- 링커 설정 부담이 NASM 계열보다 적다.
- Win32 예제가 많아 초기 창/메시지 루프를 빠르게 만들 수 있다.

Q004. 내부 해상도를 **256x144**로 확정할 것인가?

결정: **예.**

이유:

- 16:9 비율이라 현대 화면에 확대하기 쉽다.
- 프레임버퍼가 36,864픽셀이라 직접 렌더링 비용이 낮다.
- 16x16 타일 기준 16x9 타일 화면이 되어 레벨 설계가 단순하다.
- 작은 스프라이트와 제한된 팔레트가 공모전 미학에 잘 맞는다.

Q005. 오디오는 1차 목표에 **효과음만 포함하고, 음악은 2차 목표**로 둘 것인가?

결정: **예.**

이유:

- 1차 완성은 게임 루프와 전투 피드백이 먼저다.
- 효과음은 설치/공격/피해/클리어 피드백에 직접 기여한다.
- 음악까지 1차에 넣으면 waveOut, 타이밍, 패턴 데이터, 믹싱 부담이 커진다.
- 음악은 게임 루프가 완성된 뒤 남는 바이트를 채우는 exact-fill 컨텐츠로 쓰기 좋다.

Q006. 최종 스테이지 수를 **32개 마이크로 섹터**로 둘 것인가?

결정: **당시 예였으나, 이후 64개 스테이지로 상향 대체.**

이유:

- 초대 Mario는 8월드 x 4스테이지, 총 32스테이지 구조였다.
- 1.44MB는 40KiB Mario ROM의 36배에 해당한다.
- 용량상 32섹터는 충분히 가능하다.
- 각 섹터를 짧고 밀도 높은 마이크로 스테이지로 만들면 레벨 디자인 부담을 관리할 수 있다.
- 이후 타워디펜스 피벗과 1.44MB 여유를 반영해 64개 스테이지로 올렸다.

Q007. 스테이지를 **오브젝트 명령 기반 레벨 데이터**로 저장할 것인가?

결정: **예.**

이유:

- Mario식으로 전체 타일맵을 그대로 저장하지 않고 "어디에 경로/설치 영역/웨이브를 놓는다"는 명령으로 저장하면 매우 작다.
- 64개 스테이지를 넣어도 데이터 크기를 작게 유지할 수 있다.
- 같은 명령을 조합해 많은 변주를 만들 수 있다.
- 레벨 에디터 없이도 텍스트/배열 기반으로 설계하기 좋다.

Q007-A. 장르를 **64스테이지 마이크로 타워디펜스**로 피벗할 것인가?

결정: **예.**

이유:

- 64개 스테이지 목표와 잘 맞는다.
- 정교한 점프 물리보다 구현 리스크가 낮다.
- 고정 경로와 웨이브 데이터는 오브젝트 명령 기반 저장과 잘 맞는다.
- 작은 타워/적 조합으로 많은 변주를 만들 수 있다.

Q008. 타워디펜스 경로 모델을 **고정 경로**로 확정할 것인가?

결정: **예.**

이유:

- 자유 배치/동적 길찾기는 구현 복잡도가 크게 오른다.
- 고정 경로만으로도 64개 스테이지 변주가 충분하다.
- 경로 데이터를 작게 저장할 수 있다.
- 레벨 하네스에서 경로 연결성과 웨이브를 검증하기 쉽다.

Q009. 타워/적 종류를 **1차 타워 3종/적 4종, 최종 타워 5종/적 8종**으로 잡을 것인가?

결정: **예.**

이유:

- 타워 3종이면 단일/강공격/범위 역할이 분리된다.
- 최종 타워는 단일/강공격/범위/슬로우/지원으로 역할을 분리한다.
- 범위와 슬로우를 분리하면 레벨 디자인에서 순차 해금과 조합 학습이 명확해진다.
- 1차 적 4종이면 기본/빠름/탱커/특수 변주를 먼저 검증할 수 있다.
- 최종 적 8종이면 64스테이지 반복감을 줄일 수 있다.
- 적 바디는 3개 이하로 두고 팔레트/수치/행동 플래그로 다양화하면 용량 부담이 작다.
- 64스테이지를 만들기에 충분하지만, 어셈블리 구현과 밸런스가 아직 관리 가능한 범위다.
- 추가 타워/적은 exact-fill 또는 2차 목표로 남길 수 있다.

Q009-A. 64스테이지에 **순차 해금 곡선**을 둘 것인가?

결정: **예.**

이유:

- 한 번에 모든 타워/적을 보여주면 64스테이지가 산만해진다.
- 디스크 단위로 새 타워와 적을 하나씩 풀면 플레이어가 자연스럽게 학습한다.
- 스테이지별 사용 가능 타워 bitmask만 있으면 구현 부담이 작다.

Q010. 경제 모델을 **스테이지별 고정 시작 자원, 처치 보상 소량, 판매 환급 60%, Easy/Hard Mode 분리**로 잡을 것인가?

결정: **예.**

이유:

- 스테이지별 퍼즐성을 만들기 쉽다.
- 처치 보상은 플레이 중 성장감을 주되, 경제를 과하게 흔들지 않는다.
- 판매 60% 환급은 실수 복구를 허용하지만 무한 재배치를 막는다.
- Easy Mode는 진짜 금융 이자가 아니라 초당 n BYTES 지급 모드다.
- Hard Mode에서도 클리어 가능해야 하고, Easy Mode는 플레이어 선택 난이도 완화 장치로 둔다.

Q011. 클리어 기록을 **Easy Mode와 Hard Mode로 구분**할 것인가?

결정: **예.**

이유:

- Hard Mode 클리어는 챌린지 성취로 남길 수 있다.
- Easy Mode는 접근성/완화 모드로 떳떳하게 제공할 수 있다.
- 저장 시스템 없이도 현재 세션 또는 스테이지 선택 화면에서 작은 아이콘으로 표시 가능하다.

## 다음 grill-me 질문

Q012. 최고기록 공유는 **온라인 리더보드 대신 공유 가능한 기록 코드**로 처리할 것인가?

결정: **예.**

이유:

- 온라인 리더보드는 서버, 네트워크, 오류 처리, 부정 기록 검증이 필요하다.
- 1.44MB 어셈블리 단일 exe의 핵심 범위와 맞지 않는다.
- 기록 코드는 텍스트/스크린샷으로 공유 가능하고 용량 부담이 작다.
- 기록 코드에 스테이지, 모드, 남은 자원, 타워 수, 체크섬을 담으면 비교와 검증이 가능하다.

Q013. Instagram 공유는 **자동 업로드가 아니라 1080x1080 공유 카드 저장 + 기록 코드/캡션 복사**로 처리할 것인가?

결정: **예.**

이유:

- 자동 업로드는 Instagram 로그인/네트워크/정책 변화에 묶인다.
- 공유 카드 저장은 단일 exe, 오프라인, 1.44MB 철학과 맞는다.
- 1080x1080 정사각형 카드는 피드 공유에 쓰기 쉽다.
- 기록 코드를 이미지와 캡션에 같이 넣으면 스크린샷 공유만으로도 비교가 가능하다.

## 다음 grill-me 질문

Q014. 공유 카드 저장 파일 형식은 **1차 BMP, PNG는 보너스 목표**로 갈 것인가?

결정: **예.**

이유:

- BMP는 Windows에서 직접 쓰기 쉬워 초기 검증에 좋다.
- 공유 이미지는 실행 후 생성 파일이므로 제출물 1.44MB 제한에 포함되지 않는다.
- PNG는 Instagram 공유에 더 무난하지만 인코더 구현 비용이 있다.
- 외부 PNG 라이브러리는 사용하지 않는다.

Q014-A. 결과 화면에 **공유 버튼 안내 팝업**을 둘 것인가?

결정: **예.**

이유:

- 버튼을 모르면 공유 기능을 발견하기 어렵다.
- `[S] SAVE CARD   [C] COPY CODE` 같은 짧은 키 힌트면 UI 부담이 작다.
- 긴 설명문 없이 기능 발견성을 확보할 수 있다.

## 다음 grill-me 질문

Q015. 공유 카드에 **게임명, 스테이지, Easy/Hard, FREE BYTES, 타워 수, 판매 횟수, 기록 코드**를 넣을 것인가?

결정: **예.**

이유:

- 기록 비교에 필요한 최소 정보다.
- 이미지 하나만 봐도 결과의 난이도와 성과가 읽힌다.
- 기록 코드가 함께 들어가면 캡션 없이도 공유 가능하다.

Q015-A. 공유 카드의 중심 시각 요소로 **현재/결과 게임 화면 스냅샷**을 크게 넣을 것인가?

결정: **예.**

이유:

- 텍스트만 있는 기록 카드보다 공유 가치가 높다.
- 이미 렌더링한 256x144 프레임버퍼를 확대하면 구현 부담이 작다.
- 픽셀아트 미학과 잘 맞는다.
- 게임이 실제로 어떤 장면이었는지 한눈에 보인다.

Q016. 공유 카드에 들어갈 스냅샷은 **클리어 순간의 결과 화면 직전 프레임버퍼**로 할 것인가?

결정: **예.**

이유:

- 최종 배치와 적 진행 상황이 남아 있어 기록의 맥락이 보인다.
- 결과 UI가 스냅샷을 가리는 문제를 피할 수 있다.
- 구현은 클리어 판정 직전에 프레임버퍼를 한 장 복사하면 된다.

Q017. 스테이지 데이터 포맷을 **Header + path commands + build zones + waves + flags** 구조로 구체화할 것인가?

결정: **예.**

이유:

- 64개 스테이지를 오브젝트 명령 기반으로 작게 저장할 수 있다.
- Hard/Easy 자원, 사용 가능 타워 bitmask, 초당 BYTES, 웨이브 수를 header에 담기 쉽다.
- path/build/wave를 분리하면 하네스 검증이 쉽다.
- FASM 데이터 테이블로 직접 작성하기 좋다.

Q017-A. 64스테이지를 Easy/Hard로 나누어 **실질 128개 챌린지**로 볼 것인가?

결정: **예.**

이유:

- Easy/Hard는 같은 스테이지 데이터를 공유한다.
- Easy는 초당 BYTES 지급, Hard는 지급 없음으로 플레이 압박이 달라진다.
- 맵 데이터를 128개로 복제하지 않으면서 재도전 가치를 만든다.

Q017-B. Hard Mode를 **팔레트 반전/고대비 변형**으로 시각적으로 구분할 것인가?

결정: **예.**

이유:

- 새 이미지 리소스 없이 난이도 차이를 시각적으로 보여줄 수 있다.
- 팔레트 교체는 도트 그래픽과 잘 맞는다.
- 완전 반전은 UI 가독성을 해칠 수 있으므로 게임 영역 중심으로 적용한다.

Q018. 첫 구현 마일스톤을 **1스테이지, Bit Gun, Bit enemy, 고정 경로, Easy/Hard 자원 차이, Hard 팔레트 변형, 공유 카드 제외**로 잡을 것인가?

결정: **예.**

이유:

- 가장 작은 완성 루프를 검증할 수 있다.
- 공유 카드, 5타워, 8적, 64스테이지를 바로 구현하려 하면 첫 빌드가 늦어진다.
- Easy/Hard 경제 차이는 초기에 넣어야 밸런스 데이터 구조가 흔들리지 않는다.

## 다음 grill-me 질문

Q019. 구현 시작 전 첫 작업을 **FASM 확보 후 Windows x86 PE 빈 창 + 256x144 프레임버퍼 출력**으로 할 것인가?

결정: **예.**

이유:

- 현재 로컬에는 FASM이 없다.
- 가장 먼저 빌드 가능한 `.exe`와 화면 출력 경로를 증명해야 한다.
- 게임 로직은 그 다음에 붙여야 디버깅 범위가 작다.

## 다음 grill-me 질문

Q020. Windows exe 실행 검증 루트는 **1차 Wine smoke, 최종 Windows 실기기/VM 검증**으로 갈 것인가?

결정: **예.**

이유:

- 현재 개발 머신은 macOS라 Windows x86 PE를 바로 실행하기 어렵다.
- Wine은 초기 smoke test에 유용하지만 심사 환경을 완전히 대표하지 않는다.
- 최종 제출 전에는 실제 Windows 또는 Windows VM에서 실행/입력/공유 카드 저장까지 확인해야 한다.
- 따라서 개발 중 빠른 검증은 Wine, 릴리즈 검증은 Windows 실환경으로 이중화하는 편이 안전하다.

## 다음 grill-me 질문

Q021. FASM 확보 방식은 **프로젝트 로컬 고정 툴체인 + 버전/해시 기록**으로 갈 것인가?

결정: **예.**

이유:

- 시스템에 우연히 설치된 `fasm`에 의존하면 빌드 재현성이 약하다.
- `tools/` 아래에 고정 버전 툴체인을 두거나, 최소한 다운로드 버전과 SHA256을 문서화해야 한다.
- 최종 제출물에는 툴체인을 넣지 않고 `dist/`의 실행 파일만 넣으면 된다.
- macOS에서 FASM 실행 경로가 애매하면 Linux/Windows VM 또는 Docker 빌드 경로까지 같은 문서에 고정한다.

## 다음 grill-me 질문

Q022. 표준 빌드 명령은 **Makefile을 공개 인터페이스로 두고 내부는 `tools/` 스크립트로 분리**할 것인가?

결정: **예.**

이유:

- `make build`, `make size`, `make smoke`, `make release-check`처럼 외부 명령을 고정하면 사용법이 단순하다.
- 실제 구현은 `tools/` 스크립트로 빼면 macOS/Wine/Windows VM 검증 경로를 나중에 교체하기 쉽다.
- Makefile은 얇게 유지하고, 복잡한 용량 계산과 exact-fill 로직은 별도 스크립트가 맡는 편이 유지보수에 좋다.
- 최종 제출물에는 Makefile과 `tools/`를 포함하지 않는다.

## 다음 grill-me 질문

Q023. Matt Pocock 계열 스킬을 쓰기 위한 repo agent 설정을 먼저 잡을 것인가?

결정: **예. `setup-matt-pocock-skills`로 로컬 markdown 이슈 + 기본 triage label + single-context 문서 구성.**

이유:

- 현재 이 폴더는 아직 Git 저장소가 아니며 GitHub remote도 없다.
- `to-prd`, `to-issues`, `triage`, `tdd`, `diagnose`, `improve-codebase-architecture`는 issue tracker와 domain docs 위치를 알고 있을수록 잘 작동한다.
- 지금 단계에서는 GitHub Issues보다 `.scratch/` 기반 로컬 markdown 이슈가 가볍다.
- 단일 게임 프로젝트이므로 domain docs는 root `CONTEXT.md` + `docs/adr/`의 single-context 구조가 맞다.

## 다음 grill-me 질문

Q024. root agent instruction file은 **`AGENTS.md`**로 만들 것인가?

결정: **예.**

이유:

- 현재 루트에는 `AGENTS.md`와 `CLAUDE.md`가 둘 다 없다.
- Codex 작업 기준으로는 `AGENTS.md`가 더 직접적이다.
- `setup-matt-pocock-skills` 규칙상 둘 다 없으면 임의 선택하지 않고 사용자에게 확인해야 한다.
- 여기에는 agent skills 설정 블록만 두고, 자세한 운영 규칙은 `docs/agents/`에 둔다.

## 다음 grill-me 질문

Q025. 다음에 실제로 사용할 Matt Pocock 스킬은 **`grill-with-docs`로 `CONTEXT.md` 도메인 용어를 먼저 만들기**로 갈 것인가?

결정: **예.**

이유:

- 현재 문서는 상세하지만, 스킬들이 공유할 루트 도메인 glossary가 필요하다.
- `CONTEXT.md`는 구현 세부가 아니라 게임 도메인 용어를 고정하는 파일이다.
- 이후 `to-prd`, `to-issues`, `tdd`, `diagnose`가 같은 단어를 쓰게 하려면 먼저 용어를 맞추는 편이 좋다.
- 예: `Sector`, `Disk`, `BYTES`, `Easy Mode`, `Hard Mode`, `Record Code`, `Share Card`, `Tower`, `Enemy`, `Path`.

## 다음 grill-me 질문

Q026. 다음 계획 산출물은 **`to-prd` 전에 `.scratch/tracer-bullet/` 첫 playable slice 이슈 세트**로 갈 것인가?

결정: **예.**

이유:

- 현재 가장 큰 리스크는 전체 PRD 문장보다 FASM/Win32/프레임버퍼/입력/빌드가 실제로 닫히는지 여부다.
- 첫 playable slice를 이슈로 쪼개면 구현 시작 시 agent가 바로 좁은 목표를 잡을 수 있다.
- PRD는 방향을 고정하는 데 유용하지만, 이미 `docs/00`부터 `docs/15`까지 핵심 방향은 충분히 있다.
- 따라서 다음 산출물은 Track A~C를 issue tracker에 옮기는 것이 더 실행력이 높다.

## 다음 grill-me 질문

Q027. 첫 구현 진입점은 **`.scratch/tracer-bullet/issues/01-project-skeleton.md`부터 시작**으로 할 것인가?

추천 답: **예.**

이유:

- 아직 `src/`, `tools/`, `build/`, `dist/`, `Makefile`이 없으므로 구현 agent가 집을 수 있는 표준 작업면이 먼저 필요하다.
- Project Skeleton은 FASM 확보 전에도 진행 가능하다.
- 이후 이슈들이 모두 Makefile과 `dist/` 기준 검증을 전제로 하므로 선행 작업으로 적절하다.

## 다음 grill-me 질문

Q028. 64개 스테이지는 손제작 고정 캠페인으로 두고, 보너스로 **Endless 모드**와 **Random 모드**를 추가하되 각각 **24번째·48번째 스테이지 클리어로 해금**할 것인가?

결정: **예.**

세부:

- 메인: 64개 손제작 고정 스테이지 캠페인 유지(Easy/Hard, 실질 128 챌린지).
- 보너스 A — **Endless 모드**: 고정 경로 맵에서 무한 웨이브. 적 능력치를 주기적으로 스케일링하고, 생존한 웨이브 수가 점수가 된다. → **24번째 스테이지(Disk 3-8) 클리어 시 해금.**
- 보너스 B — **Random 모드**: 절차적으로 생성한 맵(경로+빌드존 랜덤) + 웨이브. 매 판 레이아웃이 달라진다. → **48번째 스테이지(Disk 6-8) 클리어 시 해금.**

이유:

- 절차적 생성이 메인인 TD는 거의 항상 로그라이트와 결합돼야 재미가 성립한다(Rogue Tower, Dome Keeper). 어셈블리로 그 메타 시스템까지 구현하면 완성 리스크가 크다. 따라서 정통 TD 노선(Kingdom Rush/Bloons)인 고정 캠페인을 메인으로 유지한다.
- 해금 게이트가 플레이어를 캠페인으로 유도하고, 두 보너스 모드를 명확한 "보상"으로 만든다.
- 24/48은 Disk 경계(3-8, 6-8)와 정확히 일치해 자연스러운 분기점이다.
- Endless는 기존 맵·웨이브 자산 + 스케일 공식만으로 가볍게 구현한다. Random은 D013/D028 명령 포맷(Header/PATH/BUILD/WAVE)을 런타임에 생성하는 작은 생성기(경로 자기교차·연결성 검증 포함)로 구현하며, 1.44MB exact-fill 보너스 콘텐츠로 적합하다.

확정 세부:

- 해금 판정 기준은 **해당 스테이지 Easy 클리어**다. (24번째를 Easy로만 깨도 Endless 해금, 48번째를 Easy로만 깨도 Random 해금. 접근성 우선.)
- 모드 정의 확정: **Endless = 고정 경로 맵 + 무한 웨이브(적 주기 스케일링, 생존 웨이브 수가 점수)**, **Random = 절차적 생성 맵(경로·빌드존 랜덤) + 웨이브**. 절차적 생성기는 Random 모드 한 곳에만 격리한다.

## 다음 grill-me 질문

Q029. 화면 표시 사양을 어떻게 확정할 것인가? (D008 재검토)

결정: **4:3 / 256x192 / 창 모드 전용 / 정수배 4배(1024x768).** (상세 D046)

이유:

- 사용자가 풀스크린 확대 시 픽셀이 과하게 커 보이는 점을 우려 → 원인은 "비율"이 아니라 "풀스크린"이므로 **창 모드 전용**으로 원천 해소한다.
- 256x144(16:9)만 16x16 타일에 깔끔히 떨어진다는 것은 오해였다. **256x192(4:3)도 16x12로 동일하게 떨어진다.**
- 진짜 판별 기준은 비율이 아니라 고정경로 여부다. 고정경로 TD는 가로가 표준이라 16:9도 정당하지만, **4:3은 세로 스위치백 경로 여지가 더 크고** 레트로 CRT 감성에 부합한다. 사용자는 레트로 외형 + 현대적 내용 톤을 원해 4:3을 선택.
- 1:1 정사각형은 가로 흐름을 죽여 제외. Share Card(1080x1080)는 별도 스냅샷이라 플레이 비율과 일치할 필요 없다.
- 표시 블록(비율+해상도+창모드+정수배)을 한 결정으로 묶어 락하고 재오픈하지 않는다.
